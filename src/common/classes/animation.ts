import type { AnimationClassObj } from '@common/types';

const animation: AnimationClassObj = {
	none: 'animate-none',
	spin: 'animate-spin',
	ping: 'animate-ping',
	pulse: 'animate-pulse',
	bounce: 'animate-bounce'
};

export default animation;
