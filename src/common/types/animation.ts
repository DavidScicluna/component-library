import { Transition } from 'framer-motion';

export type AnimationDelay = number;
export type AnimationDuration = number;
export type AnimationEasing = number[];

export type AnimationConfig = Partial<Transition>;
