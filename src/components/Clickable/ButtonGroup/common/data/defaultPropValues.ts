import { Space } from '../../../../../theme/types';
import { ButtonGroupSize } from '../../types';

export const children = [];

export const isAttached = true;

export const size: ButtonGroupSize = 'md';

export const spacing: Space = 0;
