import { Theme } from '../../../../../../../theme/types';
import { ButtonColor, Size } from '../../../../types';

export type ButtonDarkStylingProps = {
	theme: Theme;
	color: ButtonColor;
	size: Size;
};
