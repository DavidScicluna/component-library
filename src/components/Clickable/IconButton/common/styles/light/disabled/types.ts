import { Theme } from '../../../../../../../theme/types';
import { IconButtonColor } from '../../../../types';

export type IconButtonLightDisabledStylingProps = {
	theme: Theme;
	color: IconButtonColor;
	isLoading: boolean;
};
