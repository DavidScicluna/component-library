import { Theme } from '../../../../../../../theme/types';
import { IconButtonColor, Size } from '../../../../types';

export type IconButtonLightStylingProps = {
	theme: Theme;
	color: IconButtonColor;
	size: Size;
};
