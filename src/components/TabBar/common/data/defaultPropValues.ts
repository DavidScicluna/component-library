import { ColorMode } from '@chakra-ui/react';

import { TabBarColor, TabBarDirection } from '../../types';

export const color: TabBarColor = 'blue';

export const colorMode: ColorMode = 'light';

export const direction: TabBarDirection = 'bottom';
