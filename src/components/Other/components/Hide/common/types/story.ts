import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type Hide from '../../Hide';

export type HideStoryMeta = StorybookMeta<typeof Hide>;

export type HideStory = StorybookStory<typeof Hide>;
