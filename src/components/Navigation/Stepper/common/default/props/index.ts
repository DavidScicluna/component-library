import { ColorMode } from '@chakra-ui/react';

import { StepperColor } from '../../types';

export const activeStep = 0;

export const color: StepperColor = 'blue';

export const colorMode: ColorMode = 'light';
