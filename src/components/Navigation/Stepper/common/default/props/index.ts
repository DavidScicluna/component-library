import { StepperSize, StepperVariant } from '../../types';

export const activeStep = 0;
export const totalSteps = 0;

export const isDisabled = false;
export const isConsecutively = false;
export const isFitted = false;

// export const orientation: StepperOrientation = 'horizontal';

export const size: StepperSize = 'md';

export const variant: StepperVariant = 'dot';
