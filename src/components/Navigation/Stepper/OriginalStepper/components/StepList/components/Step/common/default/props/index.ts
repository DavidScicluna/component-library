import { Steps, StepStatus } from '../../types';

export const isActive = false;
export const isSelected = false;

export const hasIcon = false;

export const status: StepStatus = 'idle';

export const steps: Steps = [];
