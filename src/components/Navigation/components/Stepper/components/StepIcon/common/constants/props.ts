import type { IconVariant } from '@components/DataDisplay';

import type { StepIconDefaultElement } from '../types';

export const DEFAULT_STEP_ICON_AS: StepIconDefaultElement = 'span';
export const DEFAULT_STEP_ICON_VARIANT: IconVariant = 'unstyled';
