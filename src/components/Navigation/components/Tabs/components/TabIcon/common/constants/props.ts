import type { IconVariant } from '@components/DataDisplay';

import type { TabIconDefaultElement } from '../types';

export const DEFAULT_TAB_ICON_AS: TabIconDefaultElement = 'span';
export const DEFAULT_TAB_ICON_VARIANT: IconVariant = 'unstyled';
