import { IconFontStatus } from '../../types';

export const hasFilledIconLoaded = false;
export const hasOutlinedIconLoaded = false;

export const iconFontStatus: IconFontStatus = { filled: hasFilledIconLoaded, outlined: hasOutlinedIconLoaded };
