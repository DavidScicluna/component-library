import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import Rotate from '../..';

export type RotateStoryMeta = StorybookMeta<typeof Rotate>;

export type RotateStory = StorybookStory<typeof Rotate>;
