import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type Pop from '../../Pop';

export type PopStoryMeta = StorybookMeta<typeof Pop>;

export type PopStory = StorybookStory<typeof Pop>;
