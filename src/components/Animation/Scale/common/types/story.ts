import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import Scale from '../..';

export type ScaleStoryMeta = StorybookMeta<typeof Scale>;

export type ScaleStory = StorybookStory<typeof Scale>;
