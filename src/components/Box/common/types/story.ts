import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import Box from '../../Box';

export type BoxStoryMeta = StorybookMeta<typeof Box>;

export type BoxStory = StorybookStory<typeof Box>;
