import { ColorMode } from '@chakra-ui/react';

import { ImageEditorColor, ImageEditorMode, ImageEditorModeCropID, ImageEditorModeCropValue } from '../../types';

export const color: ImageEditorColor = 'blue';

export const colorMode: ColorMode = 'light';

export const mode: ImageEditorMode = 'crop';

export const cropID: ImageEditorModeCropID = 'square';
export const cropValue: ImageEditorModeCropValue = '1:1';

export const rotation = 0;
export const zoom = 1;
