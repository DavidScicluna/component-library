import type { IconVariant } from '@components/DataDisplay';

import type { AlertIconDefaultElement } from '../types';

export const DEFAULT_ALERT_ICON_AS: AlertIconDefaultElement = 'span';
export const DEFAULT_ALERT_ICON_VARIANT: IconVariant = 'unstyled';
