import type { AlertDefaultElement, AlertDuration, AlertStatus, AlertVariant } from '../types';

export const DEFAULT_ALERT_AS: AlertDefaultElement = 'div';

export const DEFAULT_ALERT_DURATION: AlertDuration = 0;

export const DEFAULT_ALERT_STATUS: AlertStatus = 'default';

export const DEFAULT_ALERT_VARIANT: AlertVariant = 'horizontal';
