import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type CopyButton from '../../CopyButton';

export type CopyButtonStoryMeta = StorybookMeta<typeof CopyButton>;

export type CopyButtonStory = StorybookStory<typeof CopyButton>;
