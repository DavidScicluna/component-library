import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type IconButton from '../../IconButton';

export type IconButtonStoryMeta = StorybookMeta<typeof IconButton>;

export type IconButtonStory = StorybookStory<typeof IconButton>;
