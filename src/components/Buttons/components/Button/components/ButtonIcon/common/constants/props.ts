import type { IconVariant } from '@components/DataDisplay';

import type { ButtonIconDefaultElement } from '../types';

export const DEFAULT_BUTTON_ICON_AS: ButtonIconDefaultElement = 'span';
export const DEFAULT_BUTTON_ICON_VARIANT: IconVariant = 'unstyled';
