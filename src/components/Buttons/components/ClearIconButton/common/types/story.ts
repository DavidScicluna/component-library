import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type ClearIconButton from '../../ClearIconButton';

export type ClearIconButtonStoryMeta = StorybookMeta<typeof ClearIconButton>;

export type ClearIconButtonStory = StorybookStory<typeof ClearIconButton>;
