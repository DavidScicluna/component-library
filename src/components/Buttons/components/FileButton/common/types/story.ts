import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type FileButton from '../../FileButton';

export type FileButtonStoryMeta = StorybookMeta<typeof FileButton>;

export type FileButtonStory = StorybookStory<typeof FileButton>;
