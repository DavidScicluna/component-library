import type { TextAlignClass, TextLineClampClass, ThemeLineHeight } from '@common/types';

import type { FormHelperTextDefaultElement } from '../types';

export const DEFAULT_FORM_HELPER_TEXT_AS: FormHelperTextDefaultElement = 'p';

export const DEFAULT_FORM_HELPER_TEXT_ALIGN: TextAlignClass = 'left';
export const DEFAULT_FORM_HELPER_TEXT_LINE_CLAMP: TextLineClampClass = 'none';
export const DEFAULT_FORM_HELPER_TEXT_LINE_HEIGHT: ThemeLineHeight = 'tight';
