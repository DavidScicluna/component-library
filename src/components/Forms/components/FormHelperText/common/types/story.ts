import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type FormHelperText from '../../FormHelperText';

export type FormHelperTextStoryMeta = StorybookMeta<typeof FormHelperText>;

export type FormHelperTextStory = StorybookStory<typeof FormHelperText>;
