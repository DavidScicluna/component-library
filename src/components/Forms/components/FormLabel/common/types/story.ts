import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type FormLabel from '../../FormLabel';

export type FormLabelStoryMeta = StorybookMeta<typeof FormLabel>;

export type FormLabelStory = StorybookStory<typeof FormLabel>;
