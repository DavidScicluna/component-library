import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type FormControl from '../../FormControl';

export type FormControlStoryMeta = StorybookMeta<typeof FormControl>;

export type FormControlStory = StorybookStory<typeof FormControl>;
