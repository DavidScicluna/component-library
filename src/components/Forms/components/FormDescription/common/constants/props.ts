import type { TextAlignClass, TextLineClampClass, ThemeLineHeight } from '@common/types';

import type { FormDescriptionDefaultElement } from '../types';

export const DEFAULT_FORM_DESCRIPTION_AS: FormDescriptionDefaultElement = 'p';

export const DEFAULT_FORM_DESCRIPTION_ALIGN: TextAlignClass = 'left';
export const DEFAULT_FORM_DESCRIPTION_LINE_CLAMP: TextLineClampClass = 'none';
export const DEFAULT_FORM_DESCRIPTION_LINE_HEIGHT: ThemeLineHeight = 'tight';
