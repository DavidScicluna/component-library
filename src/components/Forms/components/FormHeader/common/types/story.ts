import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type FormHeader from '../../FormHeader';

export type FormHeaderStoryMeta = StorybookMeta<typeof FormHeader>;

export type FormHeaderStory = StorybookStory<typeof FormHeader>;
