import { ColorMode } from '@chakra-ui/react';

import { TooltipColor } from '../../types';

export const arrowSize = 8;

export const color: TooltipColor = 'gray';

export const colorMode: ColorMode = 'light';

export const gutter = 8;
