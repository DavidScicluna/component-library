import { IconProps, IconRef } from '../../../../../../DataDisplay/Icon/common/types';

export type StateLabelIconProps = Omit<IconProps, 'color' | 'colorMode'>;

export type StateLabelIconRef = IconRef;
