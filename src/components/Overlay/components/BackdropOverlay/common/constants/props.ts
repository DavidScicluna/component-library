import type { ThemeBlurClass, ThemeRadius } from '@common/types';

import type { BackdropOverlayBlurType } from '../types';

export const DEFAULT_BACKDROP_OVERLAY_AMOUNT = 0.5;

export const DEFAULT_BACKDROP_OVERLAY_BLUR: ThemeBlurClass = 'none';
export const DEFAULT_BACKDROP_OVERLAY_BLUR_TYPE: BackdropOverlayBlurType = 'backdrop';

export const DEFAULT_BACKDROP_OVERLAY_RADIUS: ThemeRadius = 'none';
