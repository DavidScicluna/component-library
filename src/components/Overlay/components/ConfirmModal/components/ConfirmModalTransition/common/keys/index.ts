import { __DEFAULT_CLASS_PREFIX__ } from '@common/constants';

import type { ConfirmModalTransitionProps } from '../types';

export const __KEYS_CONFIRM_MODAL_TRANSITION_CLASS__ = `${__DEFAULT_CLASS_PREFIX__}-confirm-modal-transition`;

export const __KEYS_CONFIRM_MODAL_TRANSITION__: Array<keyof ConfirmModalTransitionProps> = [
	'transition',
	'transitionEnd'
];
