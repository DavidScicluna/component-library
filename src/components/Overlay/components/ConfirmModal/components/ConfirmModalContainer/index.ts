import * as commonKeys from './common/keys';
import type { ConfirmModalContainerProps, ConfirmModalContainerRef } from './common/types';
import ConfirmModalContainer from './ConfirmModalContainer';

// Keys
export const keys = { ...commonKeys };

// Components
export { ConfirmModalContainer };

// Component Types
export type { ConfirmModalContainerProps, ConfirmModalContainerRef };
