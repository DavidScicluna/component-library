import type { IndicatorPlacement } from '../types';

export const DEFAULT_INDICATOR_IS_VISIBLE = false;

export const DEFAULT_INDICATOR_OFFSET = 0;

export const DEFAULT_INDICATOR_PLACEMENT: IndicatorPlacement = 'top-end';
