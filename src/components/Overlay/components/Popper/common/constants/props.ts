import type { PopperPlacement } from '../types';

export const DEFAULT_POPPER_CLOSE_DELAY = 0;
export const DEFAULT_POPPER_OPEN_DELAY = 0;

export const DEFAULT_POPPER_CLOSE_ON_CLICK_OUTSIDE = true;
export const DEFAULT_POPPER_CLOSE_ON_ESC = true;

export const DEFAULT_POPPER_GUTTER = 8;

export const DEFAULT_POPPER_IS_DISABLED = false;
export const DEFAULT_POPPER_IS_OPEN = false;

export const DEFAULT_POPPER_PLACEMENT: PopperPlacement = 'top';
