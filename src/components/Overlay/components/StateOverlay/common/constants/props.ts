import type { StateOverlayState } from '../types';

export const DEFAULT_STATE_OVERLAY_HAS_GLASS = false;
export const DEFAULT_STATE_OVERLAY_IS_ALWAYS_VISIBLE = false;

export const DEFAULT_STATE_OVERLAY_STATE: StateOverlayState = 'default';
