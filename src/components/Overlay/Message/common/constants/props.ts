import { __DEFAULT_BORDER_WIDTH__ } from '@common/constants';
import type { BorderWidth } from '@common/types/classes';
import type { Radius } from '@common/types/theme';

import type { MessageVariant } from '../types';

export const __DEFAULT_MESSAGE_RADIUS__: Radius = 'xl';
export const __DEFAULT_MESSAGE_SIZE__: BorderWidth = __DEFAULT_BORDER_WIDTH__;
export const __DEFAULT_MESSAGE_VARIANT__: MessageVariant = 'transparent';
