import type { ContainerBreakpoint } from '../types';

export const DEFAULT_CONTAINER_BREAKPOINT: ContainerBreakpoint = '2xl';
export const DEFAULT_CONTAINER_IS_CONTENT_CENTERED = true;
export const DEFAULT_CONTAINER_IS_FLUID = false;
