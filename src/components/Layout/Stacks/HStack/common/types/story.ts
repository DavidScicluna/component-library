import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import HStack from '../..';

export type HStackStoryMeta = StorybookMeta<typeof HStack>;

export type HStackStory = StorybookStory<typeof HStack>;
