import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import Grid from '../../Grid';

export type GridStoryMeta = StorybookMeta<typeof Grid>;

export type GridStory = StorybookStory<typeof Grid>;
