import { ColorMode } from '@chakra-ui/react';

import { TagColor, TagSize, TagVariant } from '../../types';

export const color: TagColor = 'gray';

export const colorMode: ColorMode = 'light';

export const isActive = false;
export const isDisabled = false;
export const isFullWidth = false;
export const isClickable = false;

export const size: TagSize = 'md';

export const variant: TagVariant = 'contained';
