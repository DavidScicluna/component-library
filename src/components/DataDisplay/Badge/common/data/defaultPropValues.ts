import { ColorMode } from '@chakra-ui/react';

import { BadgeColor, BadgeSize, BadgeVariant } from '../../types';

export const color: BadgeColor = 'gray';

export const colorMode: ColorMode = 'light';

export const isLight = false;

export const size: BadgeSize = 'xl';

export const variant: BadgeVariant = 'contained';
