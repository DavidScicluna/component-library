import { BadgeSize, BadgeVariant } from '../../types';

export const isActive = false;
export const isClickable = false;
export const isCompact = false;
export const isDisabled = false;
export const isFullWidth = false;
export const isFixed = false;
export const isRound = false;

export const size: BadgeSize = 'xl';

export const variant: BadgeVariant = 'contained';
