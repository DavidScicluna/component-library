import type { ThemeSpacing } from '@common/types';

import type { CarouselDots } from '../types';

export const DEFAULT_CAROUSEL_DOTS: CarouselDots = [];
export const DEFAULT_CAROUSEL_DOTS_SIZE: ThemeSpacing = 1;
