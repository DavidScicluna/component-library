import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type HorizontalScroll from '../../HorizontalScroll';

export type HorizontalScrollStoryMeta = StorybookMeta<typeof HorizontalScroll>;

export type HorizontalScrollStory = StorybookStory<typeof HorizontalScroll>;
