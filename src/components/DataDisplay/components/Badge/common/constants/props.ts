import type { BadgeDefaultElement, BadgeSize, BadgeVariant } from '../types';

export const DEFAULT_BADGE_AS: BadgeDefaultElement = 'span';

export const DEFAULT_BADGE_IS_ACTIVE = false;
export const DEFAULT_BADGE_IS_CLICKABLE = false;
export const DEFAULT_BADGE_IS_COMPACT = false;
export const DEFAULT_BADGE_IS_DISABLED = false;
export const DEFAULT_BADGE_IS_FULLWIDTH = false;
export const DEFAULT_BADGE_IS_OUTLINED = false;
export const DEFAULT_BADGE_IS_ROUND = false;
export const DEFAULT_BADGE_IS_UPPERCASE = true;

export const DEFAULT_BADGE_SIZE: BadgeSize = 'md';

export const DEFAULT_BADGE_VARIANT: BadgeVariant = 'contained';
