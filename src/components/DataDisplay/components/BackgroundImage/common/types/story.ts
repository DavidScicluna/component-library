import type { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import type BackgroundImage from '../../BackgroundImage';

export type BackgroundImageStoryMeta = StorybookMeta<typeof BackgroundImage>;

export type BackgroundImageStory = StorybookStory<typeof BackgroundImage>;
