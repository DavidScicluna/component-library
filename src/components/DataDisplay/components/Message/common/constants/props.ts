import type { ThemeRadius } from '@common/types';

import type { MessageDefaultElement, MessageVariant } from '../types';

export const DEFAULT_MESSAGE_AS: MessageDefaultElement = 'div';

export const DEFAULT_MESSAGE_RADIUS: ThemeRadius = 'xl';
export const DEFAULT_MESSAGE_VARIANT: MessageVariant = 'transparent';
