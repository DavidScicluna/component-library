import { Space } from '../../../../../../theme/types';
import { HorizontalScrollVariant } from '../../types';

export const arrowsWidth = 0;

export const isDisabled = false;

export const spacing: Space = 2;

export const variant: HorizontalScrollVariant = 'overlay';
