import { ColorMode } from '@chakra-ui/react';

import { Space } from '../../../../theme/types';
import { CollapsibleCardColor } from '../../types';

export const color: CollapsibleCardColor = 'gray';

export const colorMode: ColorMode = 'light';

export const isActive = false;
export const isDisabled = false;
export const isDivisible = true;
export const isFullWidth = true;
export const isFixed = false;
export const isLight = true;
export const isOpen = false;

export const spacing: Space = 2;
