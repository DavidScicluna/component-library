import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import { CollapsibleCard } from '../../../..';

export type Meta = StorybookMeta<typeof CollapsibleCard>;

export type Story = StorybookStory<typeof CollapsibleCard>;
