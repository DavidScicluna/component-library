import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import { Card } from '../../..';

export type Meta = StorybookMeta<typeof Card>;

export type Story = StorybookStory<typeof Card>;
