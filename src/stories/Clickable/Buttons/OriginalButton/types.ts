import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import { Button } from '../../../..';

export type Meta = StorybookMeta<typeof Button>;

export type Story = StorybookStory<typeof Button>;
