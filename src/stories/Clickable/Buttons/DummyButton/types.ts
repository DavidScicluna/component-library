import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import { DummyButton } from '../../../..';

export type Meta = StorybookMeta<typeof DummyButton>;

export type Story = StorybookStory<typeof DummyButton>;
