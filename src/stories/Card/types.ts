import { ComponentMeta, ComponentStory } from '@storybook/react';

import Card from '../../components/Card';

export type Meta = ComponentMeta<typeof Card>;

export type Story = ComponentStory<typeof Card>;
