import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import Alert from '../../components/Alert';

export type Meta = StorybookMeta<typeof Alert>;

export type Story = StorybookStory<typeof Alert>;
