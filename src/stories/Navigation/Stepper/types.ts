import { Meta as StorybookMeta, StoryFn as StorybookStory } from '@storybook/react';

import Stepper from '../../../components/Navigation/Stepper/OriginalStepper';

export type Meta = StorybookMeta<typeof Stepper>;

export type Story = StorybookStory<typeof Stepper>;
